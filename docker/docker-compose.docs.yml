services:
  docs:
    image: ifidel_local_docs
    container_name: ifidel_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./ifidel:/app/ifidel:z
    ports:
      - '9000:9000'
    command: /start-docs
